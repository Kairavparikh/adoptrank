# AdoptRank for VS Code

Run `AdoptRank: Find for Workspace` from the Command Palette. The extension delegates safe context scanning and ranking to the installed `adoptrank-backend` CLI, so IDE and terminal behavior share one implementation.
